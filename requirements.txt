# ============================================
# بخش ۱: پیش‌نیازها (Prerequisites)
# ============================================
setuptools>=69.0.0
wheel>=0.42.0
pip>=24.0

# ============================================
# بخش ۲: هسته بک‌اند (Core Backend)
# ============================================
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic-settings==2.1.0

# ============================================
# بخش ۳: پردازش تصویر (Image Processing)
# ============================================
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80  # ✅ اضافه شد
numpy==1.26.4
Pillow==10.3.0
scikit-image==0.23.2
scipy==1.13.1  # ✅ اضافه شد

# ============================================
# بخش ۴: هوش مصنوعی و مدل‌ها (AI & Models)
# ============================================
mediapipe==0.10.8
protobuf==3.20.3  # ✅ مهم برای MediaPipe
insightface==0.7.3  # ✅ اضافه شد (جایگزین MediaPipe)
onnxruntime==1.17.1  # ✅ اضافه شد
onnxruntime-gpu==1.17.1  # ✅ برای GPU
torch==2.2.0
torchvision==0.17.0
transformers==4.37.0
sentence-transformers==2.2.2

# ============================================
# بخش ۵: پردازش زبان طبیعی (NLP)
# ============================================
langdetect==1.0.9
hazm==0.9.1
parsbert==0.1.0  # ✅ اضافه شد (مدل فارسی)

# ============================================
# بخش ۶: ابزارها و یوتیلیتی‌ها (Utilities)
# ============================================
psutil==5.9.8
requests==2.32.3
aiofiles==23.2.1
tqdm==4.66.2
pyyaml==6.0.1
python-magic==0.4.27  # ✅ تشخیص نوع فایل

# ============================================
# بخش ۷: WebSocket و ارتباطات (WebSocket & Communication)
# ============================================
websockets==12.0
python-socketio==5.10.0  # ✅ برای WebSocket

# ============================================
# بخش ۸: دیتابیس (Database)
# ============================================
psycopg2-binary==2.9.9
asyncpg==0.29.0
sqlalchemy==2.0.25
alembic==1.13.0
redis==5.0.1  # ✅ برای کش و Celery

# ============================================
# بخش ۹: تسک‌های پس‌زمینه (Background Tasks)
# ============================================
celery==5.3.4  # ✅ اضافه شد

# ============================================
# بخش ۱۰: توسعه و تست (Development & Testing)
# ============================================
jupyter==1.0.0
ipykernel==6.29.0
pytest==8.2.0
pytest-asyncio==0.23.6  # ✅ برای تست‌های async
pytest-cov==5.0.0  # ✅ برای پوشش کد
httpx==0.27.0
black==24.4.2
flake8==7.0.0
mypy==1.10.0  # ✅ برای type checking
pre-commit==3.7.0  # ✅ برای commit hooks

# ============================================
# بخش ۱۱: مستندسازی (Documentation)
# ============================================
mkdocs==1.5.3  # ✅ برای مستندات
mkdocs-material==9.5.17  # ✅ تم Material

# ============================================
# بخش ۱۲: مانیتورینگ (Monitoring)
# ============================================
prometheus-client==0.20.0  # ✅ برای metrics
opentelemetry-api==1.24.0  # ✅ برای tracing
opentelemetry-sdk==1.24.0